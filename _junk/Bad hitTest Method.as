	/*
	// this won't work...
	screen_w = Array();
	for (i=1; i<=movie_w; i++) {
		screen_w.push(i);
	}
	screen_h = Array();
	for (i=1; i<=movie_h; i++) {
		screen_h.push(i);
	}
	//
	for (bar=0; bar<2; bar++) {
		x_pos = Math.round(Math.random()*(movie_w-_root[word].vinyl._width))+10;
		y_pos = Math.round(Math.random()*(movie_h-_root[word].vinyl._height))+10;
		if ((array_search(x_pos, screen_w) != false) and (array_search(x_pos, screen_w) != false)) {
			_root[word]._x = x_pos;
			_root[word]._y = y_pos;
			screen_w.splice(x_pos -160, 
			
		}
	}
	*/